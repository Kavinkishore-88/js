//Ternary Operator

//For if else
const age = 18
const Name = age >= 18 ? "Eligible" : "Not Eligible";

console.log(Name);

//For else if
const score = 49;
const result = score >= 90 ? "A" : score >= 80 ? "B" : score >= 70 ? "C" : "F";

console.log(result);